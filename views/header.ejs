<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title || 'InkSpire' %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

<% if (success_msg && success_msg.length > 0) { %>
    <div class="flash-message flash-success"><%= success_msg %></div>
<% } %>
<% if (error_msg && error_msg.length > 0) { %>
    <div class="flash-message flash-error"><%= error_msg %></div>
<% } %>

<header>
    <nav>
        <div class="nav-left">
            <a href="/" class="logo">InkSpire</a>
        </div>
        <div class="nav-right">
            <% if (user) { %>
                <!-- Logged-in User Navigation -->
                <a href="/blogs" class="write-btn">Write Blog</a>
                <div class="dropdown">
                    <button class="dropbtn">Profile</button>
                    <div class="dropdown-content">
                        <a href="/dashboard">Dashboard</a>
                        <a href="/auth/my-articles">My Articles</a>
                        <a href="/auth/logout">Logout</a>
                    </div>
                </div>
            <% } else { %>
                <!-- Logged-out (Public) Navigation -->
                <a href="/auth/register" class="signup-btn">Sign Up</a>
                <a href="/auth/login" class="login-btn">Login</a>
            <% } %>
        </div>
    </nav>
</header>